import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

it('renders without crashing', () => {
  // This test will fail so deleting it. This is because it will call
  // fib that won't be there. In real production testing we'd probably
  // call a test stub that would return some expected data 
  
  // const div = document.createElement('div');
  // ReactDOM.render(<App />, div);
  // ReactDOM.unmountComponentAtNode(div);
});
